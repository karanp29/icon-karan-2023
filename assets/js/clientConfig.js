let webPort = 5500
let webURL = `http://localhost:${webPort}/`;
let eventRegisterUrl = `http://localhost:${webPort}}/event-register.html`
let apiPORT = 4000
let apiURL = `http://localhost:${apiPORT}/`
let createUserURL = "api/users/create";
let loginUserURL = "api/users/login";

let registerEventUrl =  "api/users/registerEvent";
let sendMailURL = "api/users/sendMail";